# Configuration migrated from ~/.bashrc
# Generated by Ansible

# NVM (Node Version Manager) configuration
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

# x-cmd tool configuration
[ ! -f "$HOME/.x-cmd.root/X" ] || . "$HOME/.x-cmd.root/X" # boot up x-cmd.

# Local environment configuration
if [ -f "$HOME/.local/bin/env" ]; then
    . "$HOME/.local/bin/env"
fi

# Google Cloud Project configuration
export GOOGLE_CLOUD_PROJECT=steel-spirit-464512-t4

# VS Code alias for WSL
alias code='/mnt/c/Users/13900H/AppData/Local/Programs/Microsoft\ VS\ Code/bin/code'

# Miniconda PATH configuration
export PATH="$HOME/miniconda/bin:$PATH"
export PATH="/home/hwt-wsl-ubt/miniconda/bin:$PATH"

# Color support for ls (zsh compatible)
if [ -x /usr/bin/dircolors ]; then
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
fi

# Alert alias for long running commands
alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'

# Load bash aliases if they exist
if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi
